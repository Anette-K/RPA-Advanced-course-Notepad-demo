This a simple process that adds two lines of text, of which one is done in the background. Then the font and fontsize of the text is changed. 
This can be useful in the business scenario of a rebranding process, where working documents and templates must be updated with a new font.

Preconditions
- Notepad is open and empty

This projekt was build using the activities available through the classic experience in UIPath.